[DB]
auth_secret = AUTH_SECRET
db_url = BOLT_URL
db_usr = USR
db_psw = PSW

[ES]
host = localhost
port = 9200
index = NEO4J_INDEX

[Cache]
type = TYPE (only simple and filesystem supported now)
dir = TMP_DIR (if using filesystem)
